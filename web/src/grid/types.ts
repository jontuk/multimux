export type Session = { id: number; tmuxName: string; toolId: number; dir: string; status: string };
export type Tool = { id: number; name: string; command: string };
export type Dir = { id: number; name: string; path: string };
